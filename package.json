{
  "name": "a14y-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/telemetry",
    "packages/apps/*",
    "packages/aliases/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present && node --test scripts/build-readmes.test.mjs",
    "build": "npm run build --workspaces --if-present",
    "docs": "node scripts/build-readmes.mjs",
    "docs:check": "node scripts/build-readmes.mjs --check"
  },
  "author": "Timothy Jordan",
  "license": "ISC"
}