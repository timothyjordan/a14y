---
import VersionSelector from '~/components/VersionSelector.astro';
import { getScorecardByVersion, isDraftScorecardVersion } from '~/lib/scorecard-data';

interface Props {
  /** Active scorecard version. */
  version: string;
}

const { version } = Astro.props;
const scorecard = getScorecardByVersion(version);
const isDraft = isDraftScorecardVersion(scorecard.version);
---

<div class="scorecard-subheader" aria-label="Scorecard version">
  <div class="scorecard-subheader-inner">
    <span class="scorecard-subheader-label">Scorecard version</span>
    <VersionSelector current={scorecard.version} />
    <span class="scorecard-subheader-meta">
      {isDraft ? 'unreleased draft' : `released ${scorecard.releasedAt}`}
    </span>
  </div>
</div>
